spring:
  datasource:
    url: jdbc:h2:file:~/ticket-local;MODE=Oracle;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  flyway:
    enabled: false

app:
  seed:
    enabled: true
    load-test-members:
      count: 2000
    # 로컬 부하 테스트 전용 회차·좌석. 운영과 같은 고정 ID 대역(910000001~)을 쓴다.
    # 회차 수를 올리면 회차당 좌석 2,000행이 늘어 기동이 느려진다.
    load-test-fixture:
      enabled: true
      performance-count: 8
  auth:
    oauth2-success-redirect-uri: ${OAUTH2_SUCCESS_REDIRECT_URI:http://localhost:3000/auth/callback}
    oauth2-failure-redirect-uri: ${OAUTH2_FAILURE_REDIRECT_URI:http://localhost:3000/auth/callback}
