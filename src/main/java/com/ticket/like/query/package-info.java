/**
 * like의 local DB 조회 구현과 읽기 모델이다.
 *
 * <p>지금은 {@link com.ticket.like.query.LikeQuery} 하나뿐이다. 파일 하나를 위한 package는 보통 만들지 않지만, {@code
 * query}는 모든 업무 module에서 같은 자리를 뜻하는 역할 이름이라 예외로 둔다.
 */
@NullMarked
package com.ticket.like.query;

import org.jspecify.annotations.NullMarked;
