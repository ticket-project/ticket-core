/**
 * like의 JPA·Querydsl 저장/조회 구현이다. 저장 계약({@code like.domain.LikeRepository})과 조회 계약({@code
 * like.query})은 밖에 있다.
 */
@NullMarked
package com.ticket.like.persistence;

import org.jspecify.annotations.NullMarked;
