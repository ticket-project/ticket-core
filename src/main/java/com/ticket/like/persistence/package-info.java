/**
 * like의 JPA·Querydsl 저장 adapter와 local DB 조회 Repository 구현이다. 저장 계약({@code
 * like.domain.LikeRepository})과 조회 결과 타입({@code like.api})은 밖에 있다.
 */
@NullMarked
package com.ticket.like.persistence;

import org.jspecify.annotations.NullMarked;
