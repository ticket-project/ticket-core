/**
 * 발권 capability다 — 확정된 OrderSeat에 발급되는 Ticket을 소유한다.
 *
 * <p>아직 use case도 endpoint도 없다. 미래를 예상해 빈 역할 package를 만들지 않는다.
 *
 * <p><b>Spring Modulith Application Module이 아니다.</b> booking 안에서 업무 단위로 탐색하기 위한 ordinary package다.
 */
@NullMarked
package com.ticket.booking.ticket;

import org.jspecify.annotations.NullMarked;
