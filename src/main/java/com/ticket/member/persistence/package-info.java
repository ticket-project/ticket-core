/**
 * member aggregate의 JPA 저장 구현이다. 저장/복원 계약({@code member.domain.MemberRepository})은 domain이 소유한다.
 */
@NullMarked
package com.ticket.member.persistence;

import org.jspecify.annotations.NullMarked;
